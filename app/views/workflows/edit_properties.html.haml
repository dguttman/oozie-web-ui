%h1
  Job Properties

%blockquote
  Edit custom workflow properties.

.form  
  = form_tag url_for(:action => :edit_properties), :method => :put

  - @properties.each do |name, value|
    = label_tag name, name
    = text_field_tag "properties[#{name}]", value
    %br

  %br
  = submit_tag "Save Changes", :class => "submit"
